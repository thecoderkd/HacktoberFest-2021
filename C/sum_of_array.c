#include <stdio.h>
int main() {
    int limit, i, sum = 0;

    printf("enter a positive integer: ");
    scanf("%d", &n);

    for (i = 1; i <= limit; i++) {
        sum += i;
    }

    printf("Sum = %d", sum);
    return 0;
}
